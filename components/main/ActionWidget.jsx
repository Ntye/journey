'use client'
import {FaBars} from "react-icons/fa";
import Link from "next/link";
import React from "react";

const SideNavbar = ({ name, color, link }) => {

  return (
    <div>
      Something About the project I think
    </div>
  );
};

export default SideNavbar;
